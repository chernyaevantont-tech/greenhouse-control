# Stale Results

This folder contains an earlier notebook run made before the GreenLightGym2
weather scenario fix. In that run, `start_date` labels did not change the
underlying weather trajectory.

Use `own-article/results_scenarios/` for article tables, figures, models, and
datasets.
