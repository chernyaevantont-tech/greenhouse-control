%% ===========================================================================
%%  04-discussion.tex  --  MDPI Agronomy manuscript, English version
%%  Body text only: no preamble, no \documentclass.
%%  Requires in the master preamble: booktabs, graphicx, amsmath
%%  (units are typeset manually as EUR\,m$^{-2}$).
%%
%%  PROVENANCE.  Every numeric value below is traceable to a file under
%%  own-article/regen/results/; the source is named in a comment beside the
%%  value or in the table comments.  Dedup rule: rows deduplicated on
%%  (method, seed, test_year) -- or on the block-specific key stated in the
%%  comment -- before averaging.  Exclusion rule: runs with truncated == True
%%  AND solver_failures >= max_solver_failures were dropped.
%%  FOUR VALUES BELOW COME FROM CODE, NOT FROM A RESULTS CSV, and are flagged
%%  here because Rule 1 (traceability to regen/results) cannot cover them:
%%   1. Library sizes 11 / 14 / 18 candidate features, and the split 3 physics
%%      features + 4 cross terms: article_experiment_utils.py:21--36
%%      (RAW_FEATURE_NAMES, PHYSICS_EXTRA_NAMES, PHYSICS_NO_CROSS_NAMES,
%%      PHYSICS_FEATURE_NAMES).  psat = e_s(t_in), vpd, S_eff = rad*(1-uThScr)
%%      are defined at article_experiment_utils.py:484--486.
%%   2. Stage-cost weights: priced branch at article_experiment_utils.py:954--968,
%%      default branch (20 : 10 : 2, i.e. 1 : 0.5 : 0.1) at line 970.  The
%%      measured marginal costs 1 : 2.97 : 0.46 are recorded in that comment as
%%      measured from regen/results/final/main.csv.
%%   3. The 16-trial hyper-parameter budget: protocol_config.py:56
%%      (hp_budget = 16) and regen/experiments_support.py:525--538
%%      (RB_TUNE_TRIALS = 16 random draws; trial 0 is the stock configuration,
%%      so 17 configurations were scored in all).
%%   4. "In seconds" / "negligible cost" for the open-loop screen: the `secs`
%%      column of regen/results/ladder_rerun/ladder_rerun*.csv (3.2 s for
%%      raw/d1/stlsq/none, 3.8 s for the frozen recipe, 20 seeds each).
%%  NO UNSOURCED VALUE REMAINS (the bracketed marker is deliberately not written out
%%  here, so that a search for it returns zero hits across the manuscript).
%%  The cross-environment magnitudes formerly carried in
%%  Section 4.6 and Section 2.9 (0 of 180 cells, mean |delta| 1.33 EUR/m2) were REMOVED
%%  on 2026-08-14: that wave is not under regen/results and no second machine exists.
%%  The limitation now rests on in-tree evidence only -- the deterministic heuristic
%%  scoring -1.2061 against -1.2264 at identical config_hash, per-season |delta|
%%  0.0031 / 0.0134 / 0.0266 / 0.0381 -- and states the cross-environment case as
%%  unmeasured rather than as measured and failed.
%%
%%  2026-08-14 -- SECTIONS 4.1 AND 4.2 REWRITTEN, MECHANISM REPLACED.
%%  The full `physics` library reached closed loop AFTER this section was drafted
%%  (regen/results/phys_lib/main_physlib.csv, 160 rows = 2 controllers x 4 seasons
%%  x 20 seeds, priced objective, zero truncated runs, zero solver aborts).  The
%%  closed-loop series is NON-MONOTONE in kappa, so the chain that formerly carried
%%  this section -- collinearity -> ill-conditioning -> unstable coefficients ->
%%  compounding rollout error -> economic loss -- is false past its open-loop half
%%  and has been removed.  It is replaced by the actuator-pathway-survival account.
%%  DO NOT REINTRODUCE: conditioning as the closed-loop mechanism; monotonicity in
%%  kappa as a closed-loop claim; "the open-loop screen reproduces the closed-loop
%%  ordering" (it identifies the winner only -- rollout ranks full physics third
%%  where the closed loop ranks it second); "only two of the three ranked libraries
%%  reached closed loop" (all three have, since 2026-08-13); the +3.05 ablation
%%  median as a live value.
%% ===========================================================================

\section{Discussion}
\label{sec:discussion}

\subsection{Why physically motivated features can hurt, and why the damage is not
monotone in how many are added}
\label{sec:disc-mechanism}

The physically motivated regressors used here---saturation vapour pressure
$e_s(T_{\text{in}})$, vapour-pressure deficit $D$ and screen-attenuated
radiation $I(1-u_{\text{ThScr}})$---are algebraic functions of variables the raw
library already contains: no new excitation, only smooth near-collinear copies
of existing columns. Adding them, and then the four bilinear cross terms, raises
the condition number of the regression monotonically, and open-loop multi-step
stability degrades monotonically along the same direction. Over the ladder's
degree-one undenoised block (Section~\ref{sec:res-ladder}),
$\kappa = 8.2 \rightarrow 24.5 \rightarrow 53.4$, median 24~h rollout error
$3.31 \rightarrow 7.11 \rightarrow 30.84$~\textdegree C and divergence
$0.000 \rightarrow 0.011 \rightarrow 0.130$. One-step error does not order the
libraries along that direction at all: it is \emph{best} for the worst-conditioned
library ($2.02$~\textdegree C) while the middle library is marginally worse than
raw ($2.17$ against $2.14$), so what the ladder establishes is a contrast between
the extreme points, not a monotone reversal.
%% ladder_rerun/ladder_rerun.csv + ladder_rerun2.csv, degree == 1, denoise ==
%% 'none', dedup on (variant, degree, optimizer, denoise, seed); n = 80 fits per
%% library (4 optimizer labels x 20 seeds).  Recomputed 2026-08-14:
%% kappa 8.2070 / 24.5167 / 53.4254; rollout median 3.3123 / 7.1094 / 30.8435;
%% diverged 0.000 / 0.011 / 0.130; one-step 2.1374 / 2.1737 / 2.0216.
Table~\ref{tab:disc-libraries} shows the same contrast on the held-out
identification block, where the three libraries score $\kappa = 8.2$, $25.4$ and
$56.2$; that block is a different sample of fits (10 replicates per fit year
rather than 20 on both), so the two sets of figures must not be mixed within one
comparison.

That much is a statement about open-loop prediction, and it holds. An earlier
version of this argument continued the chain into the closed loop: collinearity
leaves the least-squares solution weakly determined along the ill-conditioned
directions, coefficients become unstable across identification replicates, a fixed
sparsity threshold arbitrates inconsistently between near-equivalent
representations, and the resulting rollout error compounds into economic loss.
\emph{The second half of that chain does not survive measurement.} When the
extreme point of the conditioning series was finally carried into closed
loop---ensemble optimiser, threshold $0.05$, priced objective, four seasons
$\times$ 20 replicates, zero truncated runs, the feature library the only thing
that changes---the three libraries scored $+4.32$ (raw), $+0.28$ (physics without
cross terms) and $+2.75$~EUR\,m$^{-2}$ (full physics). The series is
\textbf{non-monotone in $\kappa$}: the worst-conditioned library beats the middle
one by $+2.47$~EUR\,m$^{-2}$ on average (median $+2.39$, 63 of 80 paired runs,
$p=1.3\times10^{-8}$), and it does so in each of the four seasons taken
separately.
%% Three-library one-factor comparison: phys_lib/main_physlib.csv +
%% priced_main/*.csv + priced_dagger/*.csv, dedup on (method, seed, test_year),
%% abort rule removes 0 rows, n = 80 per controller.  Recomputed 2026-08-14:
%% sindy_mpc_raw_ens +4.3173, sindy_mpc_conf +0.2818, sindy_mpc_phys_ens +2.7540.
%% Per season (raw / no-cross / full): 2020 9.10 / 3.88 / 6.35;
%% 2021 -1.56 / -3.16 / -1.91; 2022 4.62 / -0.31 / 3.07; 2023 5.10 / 0.72 / 3.51.
Conditioning therefore predicts open-loop multi-step stability cleanly and
closed-loop economics not at all. The agreement observed while only two libraries
had closed-loop controllers was a two-point coincidence, and the third point
destroys it.

What the closed-loop ordering does track is whether the boiler term survives
thresholding. The direct coefficient $\xi_{u\text{Boil}}$ is retained in 11 of 20
identification replicates with the raw library, 11 of 20 with the full physics
library, and 3 of 20 with the physics library without cross terms; the two
libraries at 11 of 20 score $+4.32$ and $+2.75$, and the one at 3 of 20 scores
$+0.28$.
%% Survival 0.55 / 0.15 / 0.55 from the same priced pool.  Survival is a property
%% of the fit and is constant across the four seasons of a replicate (verified:
%% 0 of 100 method-seed cells vary), so n = 20 replicates, not 80 runs.
Restated as a mechanism: what matters is not how well conditioned the regression
is, but whether the pathway through which an actuator reaches the controlled state
survives sparsification. A surrogate whose boiler column has been thresholded away
cannot be steered with the boiler, however accurate it is one step ahead and
however well conditioned the fit that produced it.

The controlled evidence for that reading is the single-coefficient ablation, which
this result promotes from a supporting measurement to the central one: survival
explains the closed-loop outcome and $\kappa$ does not. Reinstating the deleted
coefficient in the frozen recipe is the only randomised manipulation in the
mechanism block---library, scalers, every other term and the MPC construction held
fixed, one number changed. Under the priced objective the effect is significant and
typically small: positive in 16 of 20 replicates, $p=5.9\times10^{-4}$
($1.2\times10^{-3}$ after Holm correction over the two knock tests; $1.8\times10^{-3}$
if the family is widened to the four mechanism contrasts, as in
Figure~\ref{fig:lambda}), median $+0.21$~EUR\,m$^{-2}$ with
interquartile range $[+0.02,\,+4.49]$, mean $+1.92$ (Figure~\ref{fig:lambda}c).
The mean is nine times the median because a minority of replicates carry it, so
the median is the honest summary; the $+3.05$ median obtained under the defective
objective is superseded and must not be quoted as the size of this effect.
%% Knock-in: regen/results/priced_mech/mechanism_pricedMech*.csv (405 rows, 5
%% duplicates removed on (block, condition, seed, test_year)) against
%% regen/results/final/mechanism*.csv.  Single season, test_year = 2020, n = 20.
%% Recomputed 2026-08-14: median 0.2134, mean 1.9165, IQR [0.024, 4.491],
%% 16/20 positive, Wilcoxon p = 5.86e-4; Holm over the two knock contrasts
%% gives 1.17e-3.  Under the original objective: median 3.052, mean 2.521,
%% 17/20, p = 3.2e-4.

Survival gates the outcome; it does not order it, and two measurements say so.
Among the two libraries that keep the term, the raw one still wins by
$+1.56$~EUR\,m$^{-2}$ (median $+0.97$, 67 of 80, $p=2.5\times10^{-9}$)---and it is
also the one with the better rollout, so the open-loop criterion of
Section~\ref{sec:disc-rule} separates the two survivors where survival does not.
And in the decomposition over (library) $\times$ (term retained), the raw library
\emph{without} the term ($+3.48$~EUR\,m$^{-2}$) still beats the physics library
\emph{with} it ($+2.36$).
%% 2x2 cells: priced pool, controllers grouped by feature_variant per
%% regen/regen_config.py.  Survival is constant across the four seasons of a
%% seed, so the effective n is 19 and 20 seeds, not 76 and 320 runs.
That split is observational---survival is a property of each fit, not a randomised
factor, and it is confounded with recipe---so the conservative reading is that
survival marks which fits collapse rather than measuring the whole library effect.

Why the middle library should be worse than \emph{both} of its neighbours had one
candidate explanation, and the experiment that decides it has now been run. The
explanation was that the library without cross terms carries enough collinearity for
the threshold to destabilise the direct boiler term, but---unlike the full
library---holds no bilinear $T_{\text{in}}u_{\text{Boil}}$ term able to carry the
heating pathway once the direct term is cut: the worse-conditioned library has a
detour, the middle one has neither stability nor a detour. Two
observational figures were consistent with it, and neither is significant: inside
the full physics library, replicates that lose the direct term score no worse than
those that keep it ($+3.10$ against $+2.48$~EUR\,m$^{-2}$, 9 and 11 replicates,
$p=0.25$), whereas inside the library without cross terms losing it costs
$+2.26$ ($+2.20$ against $-0.06$, 3 and 17 replicates, $p=0.18$).
%% Mann-Whitney U on per-replicate season means, priced pool as above.  Survival
%% is constant across the four seasons of a replicate, so the unit is the
%% replicate.  Computed 2026-08-14.  Both cells are small, neither test is
%% significant and neither factor is randomised.  Do not upgrade to evidence.

\textbf{The term-deletion test rejects the detour.} Deleting exactly the bilinear
$T_{\text{in}}u_{\text{Boil}}$ feature---the only one of the four cross terms that
touches the boiler---from the full physics candidate set gives a 17-feature library
that keeps sixteen of eighteen features and essentially all of the conditioning
($\kappa = 52.3$ against $53.4$, mean over the 20 refits). Refit at the same
optimiser and threshold and simulated over four seasons $\times$ 20 replicates in
closed loop, it does not fall back towards the $+0.28$~EUR\,m$^{-2}$ and $0.15$
survival of the library without cross terms, which is what a bilinear detour
predicts. It scores $+2.11$~EUR\,m$^{-2}$ under the ensemble estimator and $+2.28$
under STLSQ, with survival $0.40$ (8 of 20 replicates, Wilson interval
$[0.22,\,0.61]$; exact McNemar test against the full library $p = 0.55$):
significantly above the middle library (mean paired difference $+1.83$, 62 of 80
pairs, $p_{\text{Holm}} = 4.4\times10^{-6}$, Holm correction over the four
contrasts of this block) and at most slightly below the full library (median
$-0.18$, $p_{\text{Holm}} = 6.4\times10^{-3}$ under the ensemble estimator; not
separable under STLSQ, $p = 0.089$). The small significant cost of the deletion in
one estimator arm suggests the bilinear term shares the threshold's pressure with
the direct term rather than replacing it. Which feature, or which correlation
pattern, makes the threshold sever the direct term in the library without cross
terms is therefore open; what the mechanism claims is untouched, and is in fact
sharpened by the fourth point: across four libraries the margin continues to track
survival---$0.15 \rightarrow +0.28$, $0.40 \rightarrow +2.11$,
$0.55 \rightarrow +2.75$ and $+4.32$~EUR\,m$^{-2}$---while neither conditioning,
one-step accuracy nor library size orders them. The cross-term block already
present in the mechanism experiment could not have substituted for this wave: that
block edits coefficients on a $\lambda=10^{-6}$ fit in which the direct boiler term
survives in all 20 replicates, so the pathway is never severed by sparsification
there, and none of its contrasts reaches significance under either objective.
%% Cross block: priced_mech/*.csv and final/mechanism*.csv, block == 'cross';
%% recipe `cross` = physics library, degree 1, stlsq, threshold 1e-6
%% (regen_config.py:120--126).  Baseline boiler-term survival in that block is
%% 1.00 in all 20 replicates -- verified 2026-08-14 -- which is exactly why it
%% cannot test a hypothesis about what happens when the term is thresholded out.
%% Paired Wilcoxon, baseline minus knockout: priced p = 0.48 (ko_cross), 0.33
%% (ko_uboil), 0.93 (ko_both); original p = 0.62, 0.09, 0.73.
%% TERM-DELETION WAVE (2026-08-15, analysed 2026-08-18): the 17-feature
%% physics_no_tuboil library (raw = 11 features, physics_no_cross = 14,
%% physics = 18, notuboil = 17; article_experiment_utils.py:21--36; the four cross
%% terms are t_S_eff, h_uVent, dc_uVent and t_uBoil = t_in*uBoil,
%% article_experiment_utils.py:487--490).  Closed loop:
%% regen/results/notuboil/main_notuboil.csv (160 rows, zero truncated/aborted);
%% open loop: regen/results/notuboil/ladder_notuboil.csv (40 fits); analysis and
%% the registered prediction: regen/analyze_notuboil.py ->
%% results/notuboil/analysis_notuboil.md.

\subsection{A cheap open-loop rule for selecting a control-oriented surrogate}
\label{sec:disc-rule}

Two things follow for practice, and they are different in kind. The first is a
screening rule: score candidates open-loop on (i) free-running multi-step rollout
error at a horizon no shorter than the one the model will be used at and (ii) the
fraction of rollouts that diverge---both computed on the identification data in
seconds, before any closed-loop season is simulated---and keep one-step error out
of the gate. The condition number belongs in the report as a diagnostic but not in
the ranking: it predicts (i) and (ii) accurately and closed-loop margin not at all
(Section~\ref{sec:disc-mechanism}). Ours scored 96 steps (24~h) against a 20-step
(5~h) control horizon---a stress test rather than a matched one, so the screening
horizon has to be reported with the score. In our ladder the
raw configuration dominates the frozen physics-informed recipe on all three
pre-registered axes at once (Section~\ref{sec:res-ladder}), and the two
controllers built on that library then take the top two places in closed loop.
The frozen recipe leads on exactly one metric, one-step accuracy (rank 8 of 72
against rank 10), which was not a gate. The screen is close to free: the whole
ladder cell costs a few seconds per fit, against a simulated season per candidate
for a closed-loop evaluation.

The strength of that claim must be stated at the resolution the three libraries
allow. Over the series, the screen \emph{identifies the closed-loop winner} and
one-step accuracy ranks that same winner \emph{last}; but neither open-loop
criterion reproduces the full closed-loop ordering, because rollout error places
full physics third where the closed loop places it second. What is established is
the choice of the top candidate and the rejection of one-step accuracy as a
selection criterion---not a rank correlation across the series.
%% Ranks over the three libraries, degree-1 undenoised block under the two sparse
%% estimators (n = 40 fits/library, ladder_rerun*.csv): one-step 1.675 (physics)
%% < 1.728 (no-cross) < 1.862 (raw); rollout median 2.67 (raw) < 10.58 (no-cross)
%% < 24.27 (physics).  Closed loop (priced pool): raw +4.32 > physics +2.75 >
%% no-cross +0.28.  So rollout and EPI agree on rank 1 and disagree on 2 and 3.
%% DO NOT restore "the screen reproduces the closed-loop ordering".

The second thing that follows is not a ranking but a check, and it is the one the
new mechanism makes necessary. After sparsification, read the fitted coefficient
matrix and confirm that every actuator the controller is allowed to move still has
a surviving path to the states it is meant to move. The check costs nothing---it
inspects numbers already computed---and it is the variable the closed loop turned
out to be sensitive to, whereas a well-conditioned regression is not a guarantee
that the check passes and an ill-conditioned one is not a guarantee that it fails.
When the check does fail, the available responses are a lower threshold, a
different basis, or a constraint protecting the actuator columns; we tested only
the first, by hand, on one coefficient, and it bought back a median of
$+0.21$~EUR\,m$^{-2}$.
%% Ladder ranks and values: regen/results/ladder_rerun/ladder_rerun.csv +
%% ladder_rerun2.csv (1440 rows = 20 seeds x 72 configs, zero duplicates).
%% Screen cost: `secs` column, 3.2 s (raw/d1/stlsq/none) and 3.8 s (frozen).
%% Caution on "72": the `constrained` and `sr3` labels are bit-identical in all
%% 360 seed-cells, so the grid holds 54 distinct fits under 72 labels.  Ranks 8
%% and 10 are unaffected (the first tied pair sits at rank 11).

Two qualifications. The ladder's rollout value is a \emph{comparator}, not a
specification: along the oracle controller's own closed-loop trajectory the same
frozen recipe predicts indoor temperature to $2.62$~\textdegree C at 24~h, four
times better than the $10.50$~\textdegree C it scores on PRBS-excited
identification data.
%% regen/results/v3_parity/parity_v3.csv (120 rows, 5 seeds, season 2020).
The held-out block supports the ordering but licenses a different claim: the
ordering is identical in-sample and held-out, yet the dominant factor is the
\emph{evaluation} year. The physics library without cross terms scores a median
rollout error of $16.9$~\textdegree C evaluated on 2018
and $2.7$~\textdegree C on 2019 whatever year it was fitted on, and in one of
the four fit$\rightarrow$evaluate directions it narrowly beats the raw library
($2.687$ against $2.722$). What is defensible is that the raw library is stable
regardless of evaluation year while the physics libraries are year-dependent.

The third piece of guidance is therefore negative: do not treat a basis function's
physical motivation as evidence that it belongs in the library. Every feature added
here is an algebraic function of columns already present, and the two groups in
which they were added both cost multi-step stability---the three non-cross features
take rollout error from $3.31$ to $7.11$~\textdegree C, the four cross terms take
it on to $30.84$---while neither group buys back a closed-loop margin above the raw
library's. Nor does the \emph{count} of added features order the damage: adding
three is worse in closed loop than adding seven
(Section~\ref{sec:disc-mechanism}), so a practitioner cannot reason from ``I added
only a few, physically sensible terms'' to ``the cost is small''. What we cannot
say is which individual feature is responsible for anything: they entered in two
groups of three and four, no feature was added or removed alone, and the one
single-feature deletion that would matter most is the untested experiment named
above. Motivation earns a candidate a place in the ladder, and nothing more.

\subsection{Sparsity is exonerated}
\label{sec:disc-sparsity}

The winner is the sparser model on both counts---11 candidate features against
14, and $19.9$ retained terms against $28.25$---so sparsity cannot be what the
physics-informed variants lost. Nor is more sparsity better within a library:
over the sparsity sweep of Table~\ref{tab:lambda} margin falls as terms are
removed, and violation steps rise with it, from about $3850$ to $9090$ between
the endpoints. The decline is not monotone across the thirteen levels, and
the between-replicate SD reaches $4.14$, larger than several of the steps, so
only the endpoints carry the comparison. The three-library series settles the
question in the other direction as well: across the ensemble triple, in which the
library is the only thing that changes, retained terms run
$20.2 \rightarrow 28.25 \rightarrow 37.2$ while closed-loop margin runs
$+4.32 \rightarrow +0.28 \rightarrow +2.75$~EUR\,m$^{-2}$. The least sparse model
of the three is not the worst; the middle one is.
%% Retained terms, ladder_rerun*.csv, degree 1, denoise none, ensemble optimizer,
%% n = 20 seeds/library: raw 20.20, physics_no_cross 28.25, physics 37.20.
%% Recomputed 2026-08-14.  The 19.9 / 28.25 pair above is a different contrast --
%% the ladder-selected raw/d1/stlsq config against the frozen ensemble recipe --
%% and the two must not be conflated.
%% regen/results/priced_mech/mechanism_pricedMech*.csv, lambda sweep block,
%% n = 20 per level, test_year = 2020 only.  Level means, lambda ascending:
%% 6.577 6.575 6.107 6.376 6.414 4.302 4.039 3.592 4.010 3.037 3.113 4.606
%% 2.433; SD 1.25 1.23 2.52 2.00 1.82 3.51 4.14 2.96 2.37 1.09 0.36 0.93 2.49.
Term count orders performance in neither direction; the basis does. The two
raw-library variants---bagged ensemble and plain sequential thresholded least
squares---cannot be separated in closed loop (median $\Delta=-0.01$, mean
$+0.48$~EUR\,m$^{-2}$, $33/80$ wins, $p=0.60$), which locates the effect in the
library rather than in the estimator. This is a failure to reject, not a
demonstration of equivalence: the mean gap is not negligible, only too disperse
to establish at $n=80$.

\subsection{What the two comparison defects imply for the field}
\label{sec:disc-defects}

Both defects (Table~\ref{tab:disc-defects}, Figure~\ref{fig:disc-corrections})
were on our side of the comparison and both inflated the result.

The reference heuristic was described as tuned and was not. Searching its four
economically relevant setpoints under exactly the budget the reinforcement-learning
agents received---16 random trials, selection on the training years only---is worth
$+3.49$~EUR\,m$^{-2}$ and moves the heuristic from last-but-three to seventh of
fifteen. The apparent advantage of the raw-library controller over it falls
from $+5.54$ to $+2.06$~EUR\,m$^{-2}$: an untuned baseline had inflated the
result $2.7$-fold.
%% Both figures use the SAME harness for the reference: stock_test and
%% tuned_test in regen/results/n2_tune/tune_rb_n2.csv (-1.2264 and +2.2613,
%% 1 deterministic run per season) against sindy_mpc_raw_ens under the priced
%% objective (+4.3173, 80 runs).  5.5437 - 3.4878 = 2.0560 exactly.  The
%% canonical rule_based rows in final/main.csv give the same contrast as
%% +5.5234 (paired, n = 80, p = 1.2e-14); the 0.02 difference is the
%% cross-harness drift disclosed in Section 4.6.  Do not mix the two.
Only 2 of the 16 random draws beat the stock setpoints: the
stock configuration sat at the 88th percentile of its own search space---not a
straw man, merely un-searched. Tuning also moved the heuristic \emph{onto} the
two-axis front, where untuned it had been dominated.
%% Default-objective front with the tuned heuristic included: sindy_mpc_raw_ens
%% (+4.07, 4467), rule_based_tuned (+2.26, 4339), ppo (+0.45, 1330).  The stock
%% heuristic (-1.23, 2813) is dominated by ppo and by rule_based.
Tuning budgets should be reported in units comparable to those granted to the
learned controllers~\cite{henderson2018}.

The second defect was internal to the controller. The MPC stage cost weighted
boiler, lamp and CO$_2$ in the ratio $1:0.5:0.1$, while the marginal costs the
economic criterion actually charges are $1:2.97:0.46$: the boiler was penalised
twice as hard as the lamp although the lamp costs three times more. Re-weighting
the energy term to the measured ratios, its total at $u=(1,1,1)$ held fixed,
improved \emph{both} axes for all eight MPC controllers, but unevenly: the five
physics-informed variants gained $+1.54\ldots+2.74$~EUR\,m$^{-2}$ and the
neural-network MPC $+1.57$, against $+0.21\ldots+0.25$ for the two raw ones.
%% Per-controller deltas (priced - default, seed- and season-matched):
%% dense +2.738, lowthr +2.676, conf_dagger +1.777, dense_dagger +1.733,
%% nn_mpc +1.565, conf +1.538, raw_ens +0.246, raw +0.214.  Sources:
%% priced_main + priced_dagger against final/main.csv, except the two raw
%% variants whose only default-objective runs are in n7/main_n7.csv.
Taking the best physics-informed variant, the raw-minus-physics gap fell from
$+3.66$ to $+1.23$~EUR\,m$^{-2}$: roughly two-thirds of what had looked like a
library effect was a defect of the objective. The repair also cost the tuned
heuristic its place on the two-axis front, yielded to a physics-informed variant
it had beaten under the defective objective---a baseline's standing in a
two-axis comparison is no more stable than the stage cost the controllers were
given. Why the correction is worth an order of magnitude more to one library
than to the other we did not measure; what follows practically is that a
controller defect can masquerade as a property of the model class, and that the
stage cost should be checked against the prices in the evaluation criterion
before any difference is attributed to a model.

\subsection{Relation to prior work}
\label{sec:disc-related}

The finding instantiates the identification-for-control programme---model
quality cannot be judged independently of the control task the model
serves~\cite{gevers1993,vandenhof1995,ljung1999,hjalmarsson2005,controlorientedsurvey2025}---and
the objective-mismatch literature of model-based reinforcement learning, where
prediction likelihood is a poor proxy for control
performance~\cite{lambert2020,wei2024,farahmand2017,jackson2022}. Our
contribution there is constructive rather than diagnostic: an open-loop screen
costing seconds per candidate identifies the closed-loop winner, whereas the
standard screen ranks that winner \emph{last}. The screen needs only free-running
rollouts, so it transfers in principle to any model class; but we exercised it on
sparse-regression libraries, and over the three of them it selects the winner
without reproducing the ordering of the two losers
(Section~\ref{sec:disc-rule}), so the correspondence is established as a choice of
top candidate, not as a rank correlation and not across model classes.
Section~\ref{sec:disc-defects} adds a second, less-studied mismatch: stage-cost
weights against the prices in the criterion, inside an otherwise correctly
specified problem. Collinearity in the library is what reweighted and
balance-guided sparse regression is designed to
mitigate~\cite{cortiella2021,balanceguided2026,belsley1980}, and our open-loop
measurements agree with that literature exactly: $\kappa$ orders multi-step
stability without exception. What we do \emph{not} find is the further step from
conditioning to closed-loop economics, where the ordering breaks
(Section~\ref{sec:disc-mechanism}). The consequence is not that library design
matters less but that it has to be scored on what the controller actually uses---a
point on which library optimisation scored by recursive long-term
prediction~\cite{yonezawa2026} converges from the opposite direction. Bagged
ensembling~\cite{fasel2022} changed neither outcome: it is statistically
indistinguishable from plain thresholded least squares on the raw library
($p=0.60$) and on the full physics library ($p=0.057$).
%% raw_ens - raw: mean +0.484, median -0.011, 33/80, p = 0.601.
%% phys_ens - phys: mean +0.278, median +0.111, 49/80, p = 0.057.
%% Paired Wilcoxon over (seed, test_year), priced pool.  Recomputed 2026-08-14.
%% Both are failures to reject, not demonstrations of equivalence.
Greenhouse comparisons of MPC and reinforcement learning have generally fixed
the model and varied the
controller~\cite{morcego2023,mallick2025,vanlaatum2025,lin2021}; the library
appears at least as consequential.

\subsection{Limitations}
\label{sec:disc-limitations}

\begin{itemize}

\item \textbf{The advantage is not Pareto-dominant.} Five controllers hold the
front; PPO incurs $1330$ violation steps against the raw library's $4130$, at
$3.86$~EUR\,m$^{-2}$ less margin. Superiority is conditional on how a grower
weights margin against corridor excursions.

\item \textbf{The MPC objective is necessarily a proxy.} The surrogate carries
no crop state, so revenue cannot be predicted and terminal biomass cannot be
priced---structural to the model class, and it blocks the natural terminal-cost
formulation.

\item \textbf{The tuned-heuristic comparison is not a paired test.} The
heuristic is deterministic and ran once per season, so its value is a constant
reused across replicates and carries no sampling uncertainty: the $+2.06$ above
is a one-sample signed-rank against that constant. Genuinely paired it collapses
to one replicate's four seasons ($+2.55$~EUR\,m$^{-2}$, $n=4$), where the
minimum attainable $p$ is $0.125$ and nothing can be significant.
%% +2.06 = mean over all 80 raw_ens runs minus the per-season constant;
%% +2.55 = the same contrast restricted to seed 0, the only seed the heuristic
%% was run at (regen/results/n2_tune/tune_rb_n2.csv).

\item \textbf{Significance is not magnitude.} Two physics-informed variants
differ by $0.0066$~EUR\,m$^{-2}$ over four seasons yet separate at $p=0.012$, so
listing both as front points is an artefact. Effect sizes accompany every
$p$-value, and Holm corrections are over a declared family of 15
controllers---another declaration moves them. This is not hypothetical: adding the
two full-\texttt{physics} controllers enlarged the family from 13 to 15 and moved
every corrected level in the main comparison, the raw-versus-physics contrast from
$7.7\times10^{-4}$ to $4.6\times10^{-4}$ among them, without any controller
crossing $0.05$.

\item \textbf{Coverage is uneven.} The neural-network MPC has 10 replicates and
40 runs against 20 and 80 elsewhere, 14 of them terminating early. The oracle
has no 2022 season---all 20 runs exhausted the solver-failure budget---but that
is an artefact: at a thousandfold budget the season completes at $-1.12$ and
$-0.56$~EUR\,m$^{-2}$, so the reported $-2.65$ is a three-season mean and the
imputed four-season value is $\approx-2.20$. One front member owes part of its
position to truncation: over complete runs its violation margin narrows from 71
to 26 steps.

\item \textbf{The mechanism rests on a three-point library comparison, which is
few.} A claim about what does and does not order closed-loop performance is being
carried by three libraries, and the third point exists on one optimiser only: the
ensemble triple is complete, whereas at threshold $0.05$ under sequential
thresholded least squares only the raw and full-physics libraries were run
($+3.83$ and $+2.48$~EUR\,m$^{-2}$), so the raw-over-physics direction replicates
but the non-monotonicity itself does not exist as a three-point series on that
axis. Boiler-term survival is \emph{measured, not manipulated}, at library level:
the libraries differ in the basis, and survival is a downstream property of the
resulting fits. The only randomised manipulation anywhere in this argument is the
single-coefficient ablation, whose median effect is $+0.21$~EUR\,m$^{-2}$---so the
mechanism is supported by one controlled contrast of modest size plus a library
pattern that is measured rather than manipulated. A fourth library has already been
added---the term-deletion probe below---and did not disturb the pattern, but the
pattern remains observational at library level.

\item \textbf{What sets boiler-term survival is unresolved.} The candidate
structural explanation---that the bilinear $T_{\text{in}}u_{\text{Boil}}$ term is
what rescues the full physics library---has been tested by deleting exactly that
feature, same optimiser and threshold, four seasons $\times$ 20 replicates in
closed loop, and is rejected (Section~\ref{sec:disc-mechanism}): the 17-feature
library keeps the full library's conditioning, open-loop instability and
closed-loop level, and its survival ($0.40$) comes nowhere near the middle
library's $0.15$. The two observational figures once offered for the detour were
non-significant at 9--11 and 3--17 replicates and now stand only as absence of
contradiction. The non-monotonicity is an established fact without an established
cause.

\item \textbf{The mechanism block is single-season.} All 800 mechanism runs are
for 2020, so neither the $\lambda$ sweep nor the ablation generalises across
seasons. The knock-in and cross-term blocks rest on different baselines
(boiler-term survival $0.15$ against $1.00$) and are not mutually comparable;
the split by term survival is observational and confounded with recipe, and only
the knock-in contrast is controlled. The library comparison, by contrast,
covers all four test seasons, including the term-deletion probe.

\item \textbf{The held-out check rests on two training years.} With 2018 and
2019 alone, year-dependence cannot be separated from chance, and the evaluation
year dominates (Section~\ref{sec:disc-rule}).

\item \textbf{The design and sensitivity blocks were never run under the priced
objective.} The support harness hard-codes the default stage cost, so those
sensitivities are default-objective results, mislabelled as priced in the run
log. Under it, perturbing identified coefficients by $\pm15\,\%$ and $\pm20\,\%$
degrades margin significantly (9 of 10 and 10 of 10 replicates worse, span of
means $12.2$~EUR\,m$^{-2}$), but as a growing minority of catastrophic runs, not
a uniform decline---the median at $\pm20\,\%$ is only $-0.31$. This finer grid
stops at $\pm20\,\%$, so the non-monotonicity the coarser canonical grid shows
at $\pm30\,\%$ is unreproduced rather than refuted.

\item \textbf{The horizon was never tuned for any MPC.} Every MPC improves at
horizon 8 over horizon 20, by $+0.29$ for the raw library and
$+1.3\ldots+2.1$ for the physics-informed variants---consistent with rollout error
compounding inside the prediction horizon, which is the open-loop half of the
argument and is unaffected by Section~\ref{sec:disc-mechanism}; and an asymmetry
running against the MPCs, which
received no hyperparameter search while the reinforcement learning agents and
the heuristic received 16 trials each.

\item \textbf{Reproducibility is within-environment only, and cross-environment
reproducibility was never measured.} The pipeline reproduces nine digests
bit-for-bit on one machine. Whether closed-loop margins survive a change of
machine is \emph{unknown}: every wave reported here ran in one environment, and
the manifests do not even record which one (Section~\ref{sec:repro}). What is
measurable in the tree is that closed-loop margins move under changes far
smaller than a change of platform---the two harnesses score the
\emph{deterministic} heuristic differently in all four seasons at identical
configuration hash, by up to $0.038$~EUR\,m$^{-2}$ and monotonically more with
each season. A controller with no stochastic element reproducing itself only to
two decimal places is the conservative reading of this limitation, and it is
enough to warrant the rule adopted: whole waves are regenerated in one
environment and never mixed cell-by-cell.
%% Heuristic drift: rule_based in regen/results/final/main.csv (+4.6335,
%% -6.9712, +1.3275, -3.8143) against stock_test in n2_tune/tune_rb_n2.csv
%% (+4.6304, -6.9846, +1.3009, -3.8524); per-season |delta| 0.0031, 0.0134,
%% 0.0266, 0.0381. Recomputed 2026-08-14.
%% The Linux/Windows figures that stood here (0 of 180 cells, mean |delta| 1.33)
%% were removed, not softened: that wave is not under regen/results, no second
%% machine exists, and an unmeasured magnitude must not be quoted. Do not restore.
The configuration hash does not discriminate between objective variants either,
so the results tree and block, not the hash, are the identifying key.

\item \textbf{Negative results stand as such.} The out-of-distribution guard
does not pay for itself, and the sign of its effect depends on which statistic is
taken --- mean against median, all pairs against completed seasons
(Section~\ref{sec:res-safety}). It truncates a left tail rather than raising the
centre, costs violations, ends $30\,\%$ of its seasons early, and rests on a
detector barely better than chance; its activation count was never recorded, so
the natural explanatory variable is missing. The extended Kalman filter observer
is worse than doing nothing ($-2.95$ against $-1.58$~EUR\,m$^{-2}$), $39\,\%$ of
its seasons terminating early.

\item \textbf{Scope.} Simulation only, one location, one planting date, one crop
model, one weather source. The ranking is not price-invariant: two different
controllers win across a nine-point grid of fruit and energy prices---though
that grid re-scores rather than re-optimises and contains neither raw-library
controller, so it does not test the ranking reported here.
%% regen/results/final/tables/sensitivity_prices.csv: fruit_price in
%% {0.8, 1.6, 3.2} x energy_scale in {0.5, 1.0, 2.0}; winners rule_based 6/9,
%% sindy_mpc_dense_dagger 3/9; sindy_mpc_raw and sindy_mpc_raw_ens absent.
No recursive feasibility or stability guarantee is claimed or
proved~\cite{mayne2000}.

\end{itemize}

%% ---------------------------------------------------------------------------
%% Source: regen/results/holdout/holdout_holdout.csv
%% (240 rows = 10 seeds x 3 libraries x 2 optimizers {stlsq, ensemble} x 4
%%  fit->evaluate directions; zero duplicates; degree 1, denoise none,
%%  rollout_horizons = 4,20,96; n = 40 per cell).  Library sizes from
%%  own-article/article_experiment_utils.py:21--36.
\begin{table}[htbp]
\centering
\caption{The selection reversal, measured on the held-out identification block.
Rollout RMSE is the median over replicates at the 96-step (24~h) horizon for
indoor temperature, with the interquartile range beside it; $\kappa$ is the
condition number of the regression matrix; ``terms'' counts non-zero
coefficients after thresholding; $n=40$ per cell (10 replicates $\times$ 2
sparse estimators $\times$ 2 fit years). $\kappa$ and the term count are
properties of the fit and so repeat across regimes. The ordering by one-step
error is the exact inverse of the ordering by every multi-step criterion, in
both regimes---but only over the two sparse estimators used here; pooled over
all four estimators of the full ladder the one-step ordering is no longer
strict. The medians overstate the separation: both physics libraries are
strongly dispersed, and the lower quartile of the no-cross library falls to the
raw library's level, which Section~\ref{sec:disc-rule} attributes to the
evaluation year. The table is an \emph{open-loop} measurement throughout:
$\kappa$ orders every column here, and it does not order closed-loop margin, where
the series is non-monotone (Section~\ref{sec:disc-mechanism}). The column is
reported as a diagnostic and must not be read as a performance ranking.}
\label{tab:disc-libraries}
\small
\begin{tabular}{lcccccc}
\toprule
Feature library & Features & $\kappa$ & Terms & One-step RMSE & Rollout RMSE, median [IQR] & Diverged \\
                &          &          &       & (\textdegree C) & (\textdegree C) & (fraction) \\
\midrule
\multicolumn{7}{l}{\emph{In-sample}} \\
Raw (physics-free)      & 11 & \textbf{8.2} & \textbf{20.6} & 1.84          & \textbf{2.62} [2.53, 2.80] & \textbf{0.000} \\
Physics, no cross terms & 14 & 25.4         & 29.7          & 1.69          & 12.14 [2.88, 14.10]        & 0.021 \\
Physics, full           & 18 & 56.2         & 36.6          & \textbf{1.64} & 21.88 [18.06, 25.58]       & 0.078 \\
\midrule
\multicolumn{7}{l}{\emph{Held-out year}} \\
Raw (physics-free)      & 11 & \textbf{8.2} & \textbf{20.6} & 1.90          & \textbf{2.72} [2.63, 2.84] & \textbf{0.000} \\
Physics, no cross terms & 14 & 25.4         & 29.7          & 1.82          & 15.42 [2.69, 21.13]        & 0.046 \\
Physics, full           & 18 & 56.2         & 36.6          & \textbf{1.77} & 21.57 [15.56, 31.74]       & 0.086 \\
\bottomrule
\end{tabular}
\end{table}

%% ---------------------------------------------------------------------------
%% Sources: regen/results/n2_tune/tune_rb_n2.csv (blocks tuned_test /
%% stock_test, 1 run per season, seed 0, and the 16 training-trial scores);
%% regen/results/priced_main/*.csv + priced_dagger/*.csv (priced objective, 600
%% rows after dedup); regen/results/final/main.csv and n7/main_n7.csv (default
%% objective).  Stage-cost weights and measured marginal costs:
%% own-article/article_experiment_utils.py:954--970 (priced branch 954--968,
%% default branch line 970).
%% The +5.54 -> +2.06 row takes BOTH ends from n2_tune so the arithmetic closes
%% (5.5437 - 3.4878 = 2.0560); see the comment in Section 4.4.  The two front
%% statements in the last row are at different objectives -- the heuristic joins
%% the front under the default objective and leaves it under the priced one.
\begin{table}[htbp]
\centering
\caption{The two comparison defects and what repairing each one cost the
headline result. Both were on the authors' side of the comparison and both had
inflated it. EPI is the seasonal margin returned by the simulator.}
\label{tab:disc-defects}
\small
\begin{tabular}{lp{4.6cm}p{4.6cm}}
\toprule
 & \emph{Defect 1}: untuned baseline & \emph{Defect 2}: mis-weighted stage cost \\
\midrule
What was wrong
 & the reference heuristic was called ``tuned'' but its setpoints were hard-coded and never searched
 & actuators weighted $1:0.5:0.1$ (boiler:lamp:CO$_2$) while the measured marginal costs are $1:2.97:0.46$ \\
\addlinespace
Repair
 & 16 random trials, the budget the reinforcement-learning agents received; selection on training years only. The MPCs received no search at all, an asymmetry running against them
 & energy term re-weighted to the measured ratios, its total at $u=(1,1,1)$ held fixed \\
\addlinespace
Direct effect
 & heuristic $-1.23 \rightarrow +2.26$~EUR\,m$^{-2}$ ($+3.49$)
 & both axes improve for all eight MPC controllers, every contrast Holm-significant, but by $+1.54\ldots+2.74$ for the physics-informed libraries against $+0.21\ldots+0.25$ for the raw ones \\
\addlinespace
Effect on the claim
 & raw-library advantage over the heuristic $+5.54 \rightarrow +2.06$~EUR\,m$^{-2}$ ($2.7\times$ smaller)
 & raw-minus-physics gap $+3.66 \rightarrow +1.23$~EUR\,m$^{-2}$ (two-thirds removed) \\
\addlinespace
Also changed
 & the heuristic moves onto the two-axis front, which it had not held untuned
 & it leaves that front again, dominated on both axes; and the boiler-coefficient ablation falls from median $+3.05$ to $+0.21$~EUR\,m$^{-2}$ \\
\bottomrule
\end{tabular}
\end{table}

%% ---------------------------------------------------------------------------
\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/fig5_corrections_waterfall.pdf}
%% FIGURE 5 of the consolidated set (figures/SPEC.md).  Two panels in one row, 17.5 cm.
%% FILE STATUS 2026-08-14: the generator make_fig5.py exists and currently writes
%%   figures/fig5.pdf + fig5.png; the SPEC name fig5_corrections_waterfall.pdf is not on
%%   disk yet.  The path above is the SPEC name and is correct as written -- the figures
%%   agent renames its output, this file does not change.
%% Waterfall / cascade.
%% Source CSVs:
%%   regen/results/final/main.csv           (rule_based, stock heuristic)
%%   regen/results/n2_tune/tune_rb_n2.csv   (blocks stock_test and tuned_test)
%%   regen/results/n7/main_n7.csv           (sindy_mpc_raw_ens, default objective)
%%   regen/results/priced_main/*.csv        (sindy_mpc_raw_ens, sindy_mpc_lowthr,
%%                                           priced objective)
%%   dedup on (method, seed, test_year); drop rows with truncated &
%%   solver_failures >= max_solver_failures.
%% y-axis: EPI difference in EUR m^-2.
%%   Panel (a) raw-library advantage over the heuristic: start bar at +5.54
%%     (vs stock), one descending bar labelled "16-trial tuning of the baseline"
%%     (-3.49), end bar at +2.06 (vs tuned); annotate 75/80 wins.
%%     BOTH ends must use n2_tune (stock_test -1.2264, tuned_test +2.2613) so
%%     the waterfall closes exactly: 5.5437 - 3.4878 = 2.0560.  Using the
%%     canonical rule_based (-1.2061, final/main.csv) for the start bar gives
%%     +5.52 and leaves a 0.03 residual -- that is cross-harness drift, not an
%%     effect, and it must not be drawn as a step.
%%     The 75/80 wins are one-sample against the per-season constant, not a
%%     paired test; label them so.
%%   Panel (b) raw-minus-physics library gap: start bar at +3.66 (default
%%     objective), one descending bar labelled "stage cost priced to the
%%     criterion" (-2.43), end bar at +1.23 (priced); annotate 62/80 wins.
\caption{Where the apparent effect went. Repairing the untuned baseline removed
a factor of $2.7$ from the raw library's advantage over the agronomic heuristic;
pricing the stage cost consistently with the evaluation criterion removed
two-thirds of the apparent library effect. Both corrections leave the direction
of the result intact and its magnitude much smaller. Source files are named in
the \LaTeX{} comment preceding this caption.}
\label{fig:disc-corrections}
\end{figure}

%% ============================================================================
%% FIGURE REMOVED 2026-08-14 (figure consolidation, ten floats -> six).
%% What stood here: `fig-knockin-distribution' / label `fig:disc-knockin' -- the paired
%% per-replicate knock-in effect under the defective and the priced objective.
%% Why: same mechanism block, same season and the same 20 replicates as the lambda sweep
%% in Sec. 3.5 (figures/SPEC.md). It is now panel (c) of Figure~\ref{fig:lambda}, and the
%% Sec. 4.1 citation above was repointed to "Figure~\ref{fig:lambda}c".
%% This ALSO moves the panel from the Discussion to the Results, where MDPI wants data to
%% live, and leaves Sec. 4.1 arguing from a cross-reference -- consistent with the three
%% passages already converted that way (REMAINING.md Sec. 5, item 9).
%% The retraction guard travels with it: +3.05 is the SUPERSEDED default-objective median
%% and may appear only beside its priced replacement +0.21; the priced mean (+1.92) is
%% nine times that median, so the mean must never be plotted alone.
%% Sources, unchanged, now documented at the Figure 3 float in 03-results.tex:
%%   regen/results/priced_mech/mechanism_pricedMech*.csv (405 -> 400 rows, dedup on
%%   (block, condition, seed, test_year), block == 'knock', test_year == 2020) and
%%   regen/results/final/mechanism*.csv (same dedup, default objective), n = 20.
%% ============================================================================

%% ===========================================================================
%% NEW REFERENCES NEEDED (not present in the statya_ru.tex bibliography)
%%
%% belsley1980 -- Belsley D.A., Kuh E., Welsch R.E. Regression Diagnostics:
%%   Identifying Influential Data and Sources of Collinearity. New York: Wiley,
%%   1980.
%%   Used for: the collinearity / condition-number diagnostic in Section~4.1 and
%%   Table~\ref{tab:disc-libraries}.  NOTE (2026-08-14): kappa is now cited only as
%%   an OPEN-LOOP diagnostic -- it orders multi-step stability and does NOT order
%%   closed-loop margin.  Do not re-cite this reference in support of a
%%   closed-loop conditioning mechanism; that mechanism was retracted.
%%
%% hjalmarsson2005 -- Hjalmarsson H. From experiment design to closed-loop
%%   control // Automatica. 2005. V. 41. No. 3. P. 393--438.
%%   Used for: the identification-for-control framing in Section~4.5, alongside
%%   gevers1993 and vandenhof1995.
%%
%% All other citations reuse keys from statya_ru.tex:
%%   gevers1993, vandenhof1995, ljung1999, controlorientedsurvey2025,
%%   lambert2020, wei2024, farahmand2017, jackson2022, cortiella2021,
%%   balanceguided2026, yonezawa2026, fasel2022, morcego2023, mallick2025,
%%   vanlaatum2025, lin2021, henderson2018, mayne2000.
%% ===========================================================================
