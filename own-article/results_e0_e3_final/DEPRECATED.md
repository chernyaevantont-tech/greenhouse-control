# DEPRECATED — не использовать для статьи

Это дерево результатов **не является каноническим** и не должно цитироваться ни в
рукописи, ни в скриптах сборки таблиц и рисунков.

**Канонический источник:** `../regen/results/final/`
(`config_hash 637c6b535a9e`), карта «утверждение → значение → файл» —
`../regen/results/final/NUMBERS.md`.

## Почему изъято

Дефекты зафиксированы в `../regen/README.md` (реестр D1–D7, G-1–G-7):

| Дефект | Суть |
|---|---|
| **D1** | Порог разреженности отсутствовал в замороженном рецепте — модель молча брала умолчание `fit_sindy` (0.05) |
| **D2** | `load_frozen_recipe()` молча подставлял зашитый словарь при отсутствии json |
| **D3** | Объявлено `TRAIN = {2018, 2019}`, исполнялось `train_scenarios()[0]` — только 2018 |
| **D4** | Бюджет отказов решателя 100 для SINDy и 10 для NN-УПМ и идеализированного — то есть для двух регуляторов, объявленных худшими |
| **D5** | Идеализированный регулятор при горизонте 12, суррогатные УПМ при 20 |
| **D7** | `EnsembleOptimizer` (pysindy 2.1.0) берёт бутстреп из глобального ГСЧ NumPy, который не инициализировался. Выживание члена котла определялось порядком выполнения, а не рецептом |

D7 означает, что результаты этого дерева **не воспроизводимы даже при фиксированном
seed**. D3 — что они не соответствуют описанию методики в самой статье.

## Что делать вместо

```bash
python regen/verify_regen.py --out regen/results/final   # приёмочные гейты
cat regen/results/final/NUMBERS.md                       # источник каждого числа
```
