
## EPI берётся из экономики симулятора gl_gym, не изобретается
Для статейных экспериментов E0–E3 первичная метрика EPI [EUR/m2·сезон] = сумма per-step `profit` из gl_gym GreenhouseReward, считываемого через `env.step(...)` info (greenlight_env.py:279 возвращает info={**reward_info,"controls":u}). В info есть profit, gains/revenue, variable_costs, heat_cost, co2_cost, elec_cost, fruit_growth_dm, *_penalty. Декомпозиция затрат и энергии (kWh/m2, kg/m2) выводится оттуда же по ценам env (fruit 1.6 €/kg, heat 0.09 €/kWh, elec 0.3 €/kWh, co2 0.3 €/kg, dmfm 0.065). Коридоры ограничений берутся с живого env: constraints_low/high порядок [CO2,T,RH] = CO2∈[300,1600] ppm, T∈[15,34]°C, RH∈[50,85]%. Реализация: protocol_config.read_env_economics(); article_experiment_utils ECON_FIELDS/_econ_row/epi_metrics; econ захватывается в collect_rule_based_dataset и всех rollout_*. Обоснование: переиспользовать экономику симулятора (van Laatum GreenLightGym), не выдумывать цены — воспроизводимо и согласовано. Состояние модели x: x[2]=tAir(°C прямо), x[0]=co2 density (→ppm через co2dens2ppm), x[15]=vapor pressure (→RH через vaporPres2rh, satVp=610.78*exp(17.2694*T/(T+238.3))), x[25]=fruit DM (cFrt).

## oracle-MPC = CEM по истинной модели env.F; grey-box = SINDy physics_no_cross
E3 регуляторы. oracle-MPC реализован как receding-horizon CEM (cross-entropy shooting) поверх истинного CasADi-интегратора симулятора env.unwrapped.F (nx=28,nu=6,p=218=10 weather+208 params; action→full control input = identity при normalize_actions=False). Тот же climate-tracking objective, что у SINDy-MPC → разрыв изолирует фиделити модели = EPI-верхний эталон относительно симулятора. ВАЖНО: встраивание F в do-mpc как discrete model (set_rhs=F(...)['xf']) технически работает символически, НО Ipopt с чувствительностями через жёсткий интегратор недопустимо медленный (первый make_step не завершился за минуты) — отвергнуто. CEM использует F.map(K) для батча; ~1.2 c/шаг (forward F ≈1.14 мс) → для article запускать на сокращённом числе сидов. grey-box-MPC реализован переиспользованием: fit_sindy(feature_variant="physics_no_cross", threshold≈1e-6) даёт плотную редуцированную линейную физ-модель, встраиваемую существующим build_mpc_controller (никакого нового MPC-кода). PPO/SAC — stable_baselines3 с MultiInputPolicy (obs — Dict space). Реализация: article_experiment_utils.rollout_oracle_mpc, train_rl/rollout_rl, paired_stats (Wilcoxon+Holm+bootstrap CI). Дисперсия — пересбор train по сидам (а не только seed прогона) — чинит претензию протокола к 07_multi_seed.

## Статья greenhouse SINDy-MPC: идём в Path A (честный бенчмарк), Phase 0 = консолидация
Решение (2026-07-02) по итогам ревизии материалов и oracle-horizon пробы: статью делаем как Path A — честный экономический in-silico бенчмарк с отрицательным результатом + интерпретируемость + safety/OOD как главный вклад. НЕ «наш метод-победитель» (центральная гипотеза §1.4 не подтверждена; SINDy-MPC мид-пак, статистически неотличим от baseline). Наивный Path B (длинный горизонт эконом. MPC) закрыт пробой: оракул с истинной моделью ни на 3–48ч не подходит к rule_based, механизм — перерасход ресурсов при равной продукции + вырождение CEM на больших размерностях; B жив только через терминальную функцию ценности (дни работы, неясный исход) — отложено.

РЕВИЗИЯ показала: числа согласованы (ноутбуки↔CSV↔снапшоты), но материалы процедурно НЕ заморожены. Критические рассогласования: (1) рецепт не заморожен — working recipe_frozen.json=physics/stlsq/0.1 (closed-loop 06-30) ≠ снапшот physics_no_cross/ensemble (open-loop 06-29) ≠ e3_main_table +0.77 (посчитан старым); run_e3_seeds читает working → повторный E3 даст другое; это же нарушение предрегистрации. (2) Ничего не в git с 26.06 (последний коммит «и»); снапшоты 0 tracked; E4-E8/runners/эконом.оракул/проба — untracked. (3) 4 каталога результатов, 2 конфликтующих «финала», results_e4_e5_final misnamed (держит E6-E8), legacy results/ stale. (4) oracle n=2, ppo n=3, SAC=NaN; sign-checks рассинхрон с equations; E4 ceiling<offline → gap_recovered невалиден; E3 одно окно (rb std=0). (5) protocol.json horizon=20 ≠ oracle horizon=12.

ПЛАН: Phase0 (дни) — заморозить ОДИН рецепт = open-loop physics_no_cross/ensemble для confirmatory (closed-loop как exploratory), свести docs; ОДИН канон results_scenarios (удалить legacy results/, снапшоты в один frozen/); закоммитить + тег baseline. Phase1 (1-2 нед, серверы) — oracle/ppo→≥10 сидов, диагностировать SAC, пересчитать sign-checks, починить E4-метрику, интегрировать oracle_horizon_sweep. Phase2 (~3-5 нед итого) — переписать под 4 сообщения Path A, поднять E5, раздел confirmatory/exploratory. Журналы: Computers and Electronics in Agriculture / Biosystems Engineering / Control Engineering Practice.

## КЛЮЧЕВАЯ находка Phase 1: нормализация obs (VecNormalize) переворачивает RL-результат — PPO −13→+3.81, конкурентоспособен
Находка 2026-07-02 (чистый confirmatory E3, physics_no_cross/ensemble, 10 сидов): починка RL (VecNormalize obs+reward — стандартная практика, добавлена при диагностике SAC-NaN) НЕ ТОЛЬКО оживила SAC, но и ПОДНЯЛА PPO с −13.0 до +3.81±1.16 — PPO теперь конкурентоспособен с лидерами (rule_based +4.65, grey_box +3.84). SAC −2.41±1.24 (работает, слаб). sindy_mpc +0.82±3.78 — ПОДТВЕРЖДАЕТ прежние +0.77 как результат confirmatory рецепта (провенанс закрыт, PHASE-1 TODO выполнен). Oracle/nn_mpc ещё считаются на server0.

СТРАТЕГИЧЕСКОЕ СЛЕДСТВИЕ: прежний тезис Path A «интерпретируемый SINDy-MPC бьёт black-box RL» РУШИТСЯ — старый PPO −13 был АРТЕФАКТОМ отсутствия нормализации obs, а не свойством RL. При честной реализации PPO (+3.81) >> sindy_mpc (+0.82). Значит: (1) Г1 ещё яснее не выполнена (PPO даёт много меньший разрыв до оракула); (2) ценность SINDy-MPC сужается до ИНТЕРПРЕТИРУЕМОСТИ + SAFETY/OOD/адаптация (пункты 1,3,4 Path A), НЕ «превосходит RL»; (3) сам факт «наивная реализация RL дала ложно-плохой результат, нормализация всё меняет» — честная методологическая находка бенчмарка (воспроизводимость RL-бейзлайнов). Нарратив 6.3 переписать: лидеры rule_based≈grey_box≈PPO (проверить Wilcoxon), sindy_mpc и oracle в середине, sac/nn_mpc слабые. Это усиливает, а не ослабляет Path A как честный бенчмарк, но окончательно закрывает любую «метод-победитель» рамку. Инфра-заметка: alias server0 (HostName server0) перестал резолвиться, тянуть по IP 192.168.1.90 с -i ~/.ssh/swarm_deploy (agroengeneer); .77 в конфиге — refused.

## Русская версия статьи — под «Автоматику и телемеханику» (Белый список, ур. 1)
Русскоязычная версия статьи оформлена под журнал «Автоматика и телемеханика» (АиТ, Белый список Минобрнауки, уровень 1; подтверждено на journalrank.rcsi.science, ISSN 0005-2310). Требования АиТ, заложенные в вёрстку: аннотация ≤100 слов одним абзацем, 3–10 ключевых слов, нумерованные разделы, номера формул СЛЕВА (leqno), список литературы в порядке упоминания в стиле АиТ (курсив авторов, «//», «V./No./P.» для иностр., «Т./№/С.» для рус.), рисунки отдельными файлами, объём ≤16 стр. двойного интервала (текущий препринт — 19 стр. при полуторном, при подаче ужать/переверстать офиц. стилевым файлом АиТ 2011 г.).

НОВЫЙ файл: own-article/paper/statya_ru.tex (компилируется Tectonic/XeLaTeX; PDF 19 стр.). Прежний own-article/paper/main_ru.tex (машинный перевод с англ.) НЕ трогали — statya_ru.tex написан заново.

Убраны «паразитные» элементы машинного перевода: рамка предрегистрации и гипотез Г1–Г4 (scorecard-таблица удалена), калька «EPI», термины confirmatory/exploratory. Установившиеся рус. термины: MPC→«управление с прогнозирующими моделями (УПМ)» (Веремей–Сотникова 2016); EPI→«экономический критерий J / сезонная прибыль»; SINDy→«разреженная идентификация нелинейной динамики» (Латиница SINDy сохранена); RL→«обучение с подкреплением»; identification-for-control→«идентификация, ориентированная на управление»; objective mismatch→«рассогласование целевых функций»; oracle→«идеализированный регулятор»; DAgger→«итеративная агрегация данных (дообучение)». Добавлены рус. якорные ссылки: Льюнг «Идентификация систем» (Наука, 1991), Сотникова–Веремей «УПМ» (2016).

Рисунки русифицированы отдельным генератором own-article/make_figures_ru.py → own-article/results_scenarios/figures/ru/ (6 PNG: Парето, λ-свип, горизонт оракула, обобщение/OOD, чувствительность, отказы). Русские оси/легенды/подписи, десятичная запятая, типографский минус, метки регуляторов = «УПМ» (согласованы с текстом). Числа сверены с каноническими CSV. Рисунок E5 (панели б/в) построен по доступному локально подмножеству прогонов → в тексте честно r≈0,67 и AUC≈0,69 (канонические 20-сид агрегаты guard: 1723→863 нарушений, p≈1e-24, оставлены). Инструмент сборки: Tectonic 0.16.9 (скачан в scratchpad, на PATH нет; server0/server1 недоступны).

## Новое название statya_ru.tex (АиТ): номинативный заголовок про порог разреженности
Название статьи заменено с вопросительно-журналистского «Когда разреженность идентифицированной модели скрывает исполнительный механизм: ...» на номинативное «Влияние порога разреженности при идентификации суррогатной модели на экономическую эффективность прогнозирующего управления микроклиматом теплицы». Обоснование: (1) конструкция «Когда ... скрывает» — калька с англоязычных заголовков, для АиТ (Белый список, уровень 1) принят именной стиль без двоеточий; (2) акцент на «порог разреженности» (гиперпараметр) соответствует утверждённому нарративу владельца: качество потеряно из-за ошибки критерия отбора, а не из-за разреженности/прозрачности как таковых; (3) заголовок отражает главный результат (λ-развёртка → обнуление члена котла → падение J в замкнутом контуре).

## Стратегия ответа на рецензию statya_ru.tex (major revision): триаж по 4 классам
Получена развёрнутая рецензия на statya_ru.tex (12 основных + частные + формальные замечания, вердикт «существенная переработка»). План согласования — own-article/paper/otvet_recenzentu_ru.md. Принцип: НИ ОДНО действие не меняет уже полученные результаты (новые эксперименты дают новые данные; переотчёт представляет те же данные корректнее; остальное — формулировки/оформление). Триаж на 4 класса: [В] новые вычисления на кластере — (1) knock-out/knock-in ТОЛЬКО коэффициента Ξ(uBoil→t_in) с сохранением остальных членов [код это поддерживает: bundle.model.coefficients() правится точечно, MPC берёт матрицу напрямую], (3) перенос главного E3 на ≥3 погодных года (2021–2023) с иерархическим бутстрепом по сезонам, (6) разделение ошибки модели и ошибки оптимизатора для оракула (общий решатель + воспроизведение одной последовательности действий), (7) все 20 запусков SAC вместо 10 (survivorship); [П] переотчёт имеющихся данных — (2) TOST/не-меньшая эффективность с границей 0,5 евро/м² вместо вывода по p=0,26, (8) размеры эффекта+ДИ+медианы/IQR, (11) Парето с неопределённостью; [Т] только текст — (4) масштабирование/библиотека SINDy+билинейность t·uBoil, (5) полная постановка УПМ, смягчение причинности→ассоциация и «безопасность»→«эмпирическое снижение нарушений», (10) J→«операционная маржа по переменным затратам», частные+формальные (аннотация ≤100 слов, объём ≤16 стр двойного интервала — резать ~треть, стилевой файл АиТ 2023 не 2011, замена arXiv GreenLight-Gym на IFAC-PapersOnLine 2025 DOI); [Р] решения автора — (12) что резать/выносить в отд. публикацию (EKF/OOD/отказы/чувствительность/оракул), публикация кода с DOI (Zenodo/тег), данные авторов. Где возражаем: #6 (логика рецензента не позволяет требовать от оракула и роль верхней границы), #11 (SAC на фронте — согласны, уберём практической оговоркой). Пользователь выбрал начать с rebuttal-документа (утверждение стратегии) до правок статьи и запуска экспериментов.

## Канонический источник E3 — чистый регген на кластере (разрешает конфликт двух «финалов»)
Обнаружены две конфликтующие таблицы E3: (1) e3_main_table.csv + e3_stats_vs_rulebased.csv + e3_pareto_table.csv (коммит 8373f35, 02.07) — их использует статья statya_ru.tex и генератор рисунков make_figures_ru.py; заголовок conf_dagger=5,23±2,40, Δ+0,59, p_Holm=0,26; априорный confirmatory=1,18±4,33; есть oracle 1,99 и nn_mpc −5,09. (2) e3_main_table_20seed.csv + e3_stats_20seed.csv (коммит 2d5eb9a, 03.07, «20-seed regen — E3 headline+Pareto+stats») — новее, но частичные (нет oracle/nn/exploratory); conf_dagger=4,66±3,17, Δ+0,03, p_Holm=0,57; априорный=0,29±3,92. Остальные строки (rule_based, dense, grey_box, ppo, dense_dagger) в обеих совпадают дословно. Регген 03.07 не переключил ни статью, ни рисунки — миграция незавершённая. DECISIONS.md уже фиксирует «2 конфликтующих финала» и что рецепт не заморожен консистентно (working recipe_frozen.json=physics/stlsq/0.1 ≠ snapshot physics_no_cross/ensemble ≠ e3_main_table).

РЕШЕНИЕ (17.07, владелец): каноничным считать ЧИСТЫЙ регген на кластере agroengineer-cluster — один замороженный рецепт, 20 сидов, все 10 регуляторов (вкл. oracle/nn), из одного источника выводятся текст, рисунки и статистика. Это заодно закрывает замечание рецензента 3 (мультисезон: тот же прогон по нескольким погодным годам) и снимает замечание 13 (воспроизводимость/конфликтующие CSV). Следствие: заголовочные числа статьи (5,23 / 1,18 / p=0,26) будут заменены на результат чистого реггена; правки статистики (замеч. 2 TOST, 8 размеры эффекта/медианы, 11 Парето) и заголовочные числовые ячейки ОТЛОЖЕНЫ до реггена. Число-независимые правки (постановка УПМ, масштабирование/библиотека SINDy, переименование J→операционная маржа, ослабление формулировок о причинности/безопасности, явное уравнение температуры, конфиги PRBS/обусловленность/NN, формальные требования) ведутся сейчас. Прежде чем запускать регген — заморозить ОДИН рецепт (устранить рассогласование working≠snapshot из DECISIONS.md).
