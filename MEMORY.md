
## bugfix
Среда sindylom venv новее протокольных пинов → пришлось адаптировать article_experiment_utils.py под установленные версии (downgrade рабочего venv с gl_gym не делали). Фиксы: (1) sklearn 1.8 удалил mean_squared_error(squared=False) → заменено на np.sqrt(mean_squared_error(...)) в evaluate_sindy/evaluate_nn_surrogate. (2) pysindy 2.1: SINDy.__init__ больше НЕ принимает feature_names → перенесено в model.fit(..., feature_names=STATE_NAMES+feature_names). (3) pysindy 2.1 SR3/ConstrainedSR3 переименовали threshold→reg_weight_lam, nu→relax_coeff_nu; TrappingSR3 отсутствует (для "constrained/stability" используем ConstrainedSR3). (4) do-mpc/Ipopt глушится через set_param(nlpsol_opts={"ipopt.print_level":0,"ipopt.sb":"yes","print_time":0}). (5) SB3 требует MultiInputPolicy (obs env — Dict). (6) venv uv-managed без pip → ставить через `uv pip install --python <venv> ...`; нет nbformat/nbclient/stable_baselines3 → доустановлены; зарегистрирован ipykernel name=python3 для nbclient. Запуск ноутбуков: own-article/run_all_notebooks.py, FAST_MODE через env ARTICLE_FAST (default 1=смоук, 0=article-grade).
